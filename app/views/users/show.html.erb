<% provide(:title, @user.name) %>
<div class="row">
  <div class="col-lg-6 col-lg-offset-3 user_info" >

  	<%= gravatar_for @user , options = { size: 150, class:"user_info-gravatar" } %>
  
  </div>

  </div>
    <div class="row">
    	 <div class="col-lg-6 col-lg-offset-3 user_info">

      <h1 class="user-info-h1">
        <%= @user.name %>
      </h1>
      </div>   
    </div>

    <div class="row">

    	<div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2 ">
	    	<div class="table-responsive">          
	  			<table class="table">
	    
	    			<tbody>
	      				<tr>
	        				<td>First Name:</td>
	        				<td class="td2"><%= @user.name %></td>
	        			</tr>

	        			<tr>
	        				<td>Last name:</td>
	        				<td class="td2"><%= @user.last_name %></td>
	        			</tr>

	        			<tr>
	        				<td>Email:</td>
	        				<td class="td2"><%= @user.email %></td>
	        			</tr>

	        			<tr>
	        				<td>personel Number:</td>
	        				<td class="td2"><%= @user.personel_number %></td>
	        			</tr>
	        		</tbody>

				 </table>
			</div>

		</div>

    </div>

    <div class:"row">
    	<div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2 ">
    		<% if current_user.admin? %>
     <%= link_to "delete User", @user, method: :delete,:class => "btn btn-primary reg-form-button delete-button",
                                  data: { confirm: "Are You sure you want to delete this user ?" } %>
  		<% end %>
   		 	<%= link_to "edit information", edit_user_path(@user), :class => "btn btn-primary reg-form-button" %>
   		 </div>
  	</div>
